# ===== Master Configuration =====
port 6379
bind 0.0.0.0
protected-mode no
daemonize no
dir /data
dbfilename dump.rdb
appendonly yes
appendfilename "appendonly.aof"

# ===== Replication =====
# Master이므로 replicaof 설정 없음
